name: CI

on:
    push:
        branches: ["**"]
        tags-ignore: ["v*"]
    pull_request:

env:
    FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
    build:
        name: Build
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node@v4
              with:
                  node-version: "22"
                  cache: "npm"

            - name: Install dependencies
              run: npm ci

            - name: Type check
              run: npm run lint

            - name: Run tests
              run: npm test

            - name: Build all cards
              run: npm run build

            - name: Upload dist
              uses: actions/upload-artifact@v4
              with:
                  name: dist
                  path: dist/insight-card.js
                  retention-days: 7
